name: C Workflow
 
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: Print Hello Message
      run: echo "hello"

      
    - name: Git clone repos
      uses: actions/checkout@v1
      
    - name: List current folder 
      run: ls -la 
    - name: List current folder 
      run: |
        cd lab_1 
        ls -la
        make
        ls -la 
    - name: valgrind
      run: |
        ls -la 
        sudo apt-get install -y valgrind
        sudo apt install valgrind
        ls -la
        cd lab_1
        ls -la
        chmod a+x ./main.c
        valgrind -v --leak-check=full --show-leak-kinds=all ./main.c

        chmod a+x ./computing.c
        valgrind ./computing.c

  

